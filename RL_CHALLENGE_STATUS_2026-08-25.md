# RL挑战场景审计（2026-08-25）

## 目的与边界

`rl_challenge_v1` 专门考察动态障碍下的有限前瞻控制，必须与标准四场景分开
报告。场景从原规划器认证的 Whole-body 和 Confined split 派生，不使用任何
控制器的成功或失败作为生成筛选条件。测试集有200个场景，在算法冻结前不运行。

场景由两类组成：

- `timed_crossing`：障碍按名义机械臂到达时间横穿关键阻挡位置；
- `closing_gate`：走廊障碍对在名义中点时刻收紧，随后反弹重新开放。

训练、验证、测试规模分别为120、40、200，随机种子为20260825。

## 验证集难度审计

在40个验证场景上使用完整500步协议评测确定性控制器：

| 方法 | 总成功 | Timed crossing | Closing gate |
|---|---:|---:|---:|
| PD | 0/40 | 0/20 | 0/20 |
| Gradient Projection | 35/40 | 15/20 | 20/20 |
| CBF-QP | 37/40 | 18/20 | 19/20 |
| Adaptive Gradient-CBF | 36/40 | 17/20 | 19/20 |

原始结果位于服务器：

```text
/root/autodl-tmp/manipulator/results/rl_challenge_v1_validation/
```

## 结论

v1验证了动态障碍观测、速度传播、场景切换和统一评测链路，但不足以支撑
“RL解决局部方法无法解决的问题”的论文主张：CBF-QP仍达到92.5%，Closing
gate对Gradient Projection仍为100%。它应保留为动态反应基准，当前不启动
正式RL训练，也不查看其200场景测试结果。

v2需要改变决策结构，而不是只增加速度或障碍数量：

1. 转弯窄通道：要求机械臂在入口前改变肘部构型；
2. 镜像双通路：左右可行方向随机，眼前较宽通路在后段封闭；
3. 非单调参考：成功路径必须先增大末端跟踪误差再回归名义轨迹；
4. 使用与被测方法无关的自由路径规划器认证可解性；
5. 只用训练/验证集调场景和算法，冻结后一次性运行测试集。

RL在v2中的职责应限制为绕行侧、短期子目标或参考松弛等高层决策；底层CBF
提供瞬时安全，避免让策略重新学习已知运动学和基础碰撞约束。
